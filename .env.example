# Datenbank (lokal: MariaDB)
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=3dprint
DB_USER=root
DB_PASS=

# E-Mail (lokal leer lassen = mail() Fallback)
MAIL_HOST=
MAIL_PORT=587
MAIL_USER=
MAIL_PASS=
MAIL_FROM=noreply@example.com
MAIL_FROM_NAME=3D-Druckservice

# Admin-Passwort (bcrypt-Hash generieren mit: php -r "echo password_hash('deinPasswort', PASSWORD_BCRYPT);")
ADMIN_PASSWORD_HASH=

# Admin E-Mail für Benachrichtigungen
ADMIN_EMAIL=

# App-URL (für Magic Links in E-Mails)
APP_URL=http://localhost:8000
